-- This file is copied into product repos by the product itself in normal use.
-- The factory test fixture validates the expected contract shape.
return {
    ok = true,
    message = "factory fixture only",
}
